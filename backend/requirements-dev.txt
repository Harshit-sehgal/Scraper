# =============================================================================
# DataForge Scraper — Python Development Dependencies (lower-bound convenience)
# =============================================================================
# This file is a thin wrapper that defers ALL pinned versions to
# `requirements-dev.lock.txt`. It is kept only for callers that expect
# a top-level `requirements-dev.txt` (e.g. legacy README snippets,
# optional CI suites).
#
# Canonical sources are:
#   - backend/requirements-dev.in          (dev tooling intent, bounded)
#   - backend/requirements-dev.lock.txt     (dev, pinned)
#
# Update:
#   pip install -r requirements-dev.lock.txt
# =============================================================================

-r requirements-dev.lock.txt
