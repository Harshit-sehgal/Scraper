# =============================================================================
# DataForge Scraper — Python Dependencies (lower-bound convenience entry)
# =============================================================================
# This file is a thin wrapper that defers ALL pinned versions to
# `requirements.lock.txt`. It is kept only for callers that expect
# a top-level `requirements.txt` (e.g. legacy README snippets,
# human-friendly "pip install -r requirements.txt" usage).
#
# New contributors and CI should use the canonical files:
#   - backend/requirements.in          (production intent, bounded ranges)
#   - backend/requirements.lock.txt    (production, pinned)
#   - backend/requirements-dev.in      (dev tooling intent, bounded ranges)
#   - backend/requirements-dev.lock.txt (dev, pinned)
#
# Update:
#   pip install -r requirements.lock.txt    # production
#   pip install -r requirements-dev.lock.txt # development
# =============================================================================

-r requirements.lock.txt
